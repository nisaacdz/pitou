right click options {
  duplicate,
  open,
  open with
  copy path
  copy
  cut
  info
  delete
  archive,
  extract,
  rename,
  share,
}

search settings {
  max finds: 100,
  input
  search kind (EndsWith(), BeginsWith(), Contains(), Regex(), Similar())
  case sensitive?
  filter?
  
}